import { modelsHealthClient } from './models-health-client';

describe('modelsHealthClient', () => {
  it('should work', () => {
    expect(modelsHealthClient()).toEqual('models-health-client');
  });
});
